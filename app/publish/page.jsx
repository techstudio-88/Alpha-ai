"use client";import{useState}from"react";import{createClient}from"@supabase/supabase-js";export default function Page(){const[c,setC]=useState("");const[p,setP]=useState("youtube");const[t,setT]=useState("");const[o,setO]=useState(null);async function go(){const s=await createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY||process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY).auth.getSession();const r=await fetch("/api/publish",{method:"POST",headers:{Authorization:"Bearer "+s.data.session?.access_token,"content-type":"application/json"},body:JSON.stringify({clip_id:c,platform:p,title:t})});setO(await r.json())}return <main className="seo-page"><h1>Publish & Schedule</h1><p>Queue a clip for publishing. External provider OAuth credentials are required for delivery.</p><input value={c} onChange={e=>setC(e.target.value)} placeholder="Clip ID"/><input value={t} onChange={e=>setT(e.target.value)} placeholder="Post title"/><select value={p} onChange={e=>setP(e.target.value)}><option>youtube</option><option>instagram</option><option>tiktok</option></select><button onClick={go}>Queue publish</button>{o&&<pre>{JSON.stringify(o,null,2)}</pre>}</main>}