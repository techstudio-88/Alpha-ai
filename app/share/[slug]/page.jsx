import{createClient}from"@supabase/supabase-js";export const dynamic="force-dynamic";export default async function Share({params}){const p=await params;const client=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY||process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY);const{data}=await client.from("public_shares").select("slug,clip_id,branding_enabled,view_count,clips(title,start_seconds,end_seconds,project_id)").eq("slug",p.slug).eq("enabled",true).maybeSingle();if(!data)return <main style={{padding:60,fontFamily:"Arial"}}><h1>Share not found</h1><p>This Alpha.ai share is no longer available.</p></main>;return <main style={{maxWidth:900,margin:"0 auto",padding:"60px 24px",fontFamily:"Arial"}}><div style={{fontSize:13,opacity:.65}}>ALPHA.AI</div><h1>{data.clips?.title||"Shared clip"}</h1><p>AI-edited short-form clip.</p><p style={{opacity:.7}}>Share views: {data.view_count||0}</p>{data.branding_enabled&&<p style={{opacity:.65}}>Made with Alpha.ai</p>}<a href="/">Create your own clips with Alpha.ai →</a></main>}