"use client";import{useEffect}from"react";export default function ShareView({slug,videoUrl,branding,initialViews}){useEffect(()=>{fetch("/api/share/"+encodeURIComponent(slug)+"/view",{method:"POST"}).catch(()=>{})},[slug]);return <><p>AI-selected short-form clip.</p>{videoUrl?<video controls playsInline preload="metadata" src={videoUrl} style={{width:"100%",borderRadius:16,background:"#111"}}/>:<p>This shared clip is no longer available.</p>}<p>{initialViews} views</p>{branding&&<small>Made with Alpha.ai</small>}</>}