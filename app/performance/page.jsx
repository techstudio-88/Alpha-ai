"use client";import{useState}from"react";export default function Page(){const[b,setB]=useState("");const[p,setP]=useState("youtube");const[c,setC]=useState("");const[r,setR]=useState(null);async function run(){setR(null);const token=(await import("@supabase/supabase-js")).createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY||process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY);const s=await token.auth.getSession();const x=await fetch("/api/performance/analyze",{method:"POST",headers:{Authorization:"Bearer "+s.data.session?.access_token,"content-type":"application/json"},body:JSON.stringify({baseline_views:b,platform:p,clip_id:c})});setR(await x.json())}return <main className="seo-page"><h1>Video Performance Intelligence</h1><p>Enter a normal baseline and a clip ID to estimate a range using Alpha.ai clip signals and available historical data.</p><input placeholder="Baseline views" value={b} onChange={e=>setB(e.target.value)}/><input placeholder="Clip ID" value={c} onChange={e=>setC(e.target.value)}/><select value={p} onChange={e=>setP(e.target.value)}><option>youtube</option><option>instagram</option><option>tiktok</option></select><button onClick={run}>Analyze</button>{r&&<pre>{JSON.stringify(r,null,2)}</pre>}</main>}