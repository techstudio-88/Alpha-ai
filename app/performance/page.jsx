"use client";import{useEffect,useState}from"react";import{createClient}from"@supabase/supabase-js";const sb=()=>createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY||process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY);export default function Page(){const[w,setW]=useState([]),[wid,setWid]=useState(""),[cid,setCid]=useState(""),[platform,setPlatform]=useState("youtube"),[baseline,setBaseline]=useState(""),[r,setR]=useState(null);useEffect(()=>{(async()=>{const s=await sb().auth.getSession(),x=await fetch("/api/workspaces",{headers:{Authorization:"Bearer "+s.data.session?.access_token}}),d=await x.json();setW(d.workspaces||[]);if(d.workspaces?.[0])setWid(d.workspaces[0].id)})()},[]);async function run(){const s=await sb().auth.getSession(),x=await fetch("/api/performance/analyze",{method:"POST",headers:{Authorization:"Bearer "+s.data.session?.access_token,"content-type":"application/json"},body:JSON.stringify({workspace_id:wid,clip_id:cid,platform,baseline_views:Number(baseline||0)})});setR(await x.json())}return <main className="seo-page"><h1>Performance Intelligence</h1><p>Workspace-scoped estimate using stored clip signals and historical performance.</p><select value={wid} onChange={e=>setWid(e.target.value)}>{w.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><input value={cid} onChange={e=>setCid(e.target.value)} placeholder="Clip ID"/><input value={baseline} onChange={e=>setBaseline(e.target.value)} placeholder="Typical views"/><select value={platform} onChange={e=>setPlatform(e.target.value)}><option>youtube</option><option>instagram</option><option>tiktok</option></select><button onClick={run}>Analyze clip</button>{r&&<pre>{JSON.stringify(r,null,2)}</pre>}</main>}