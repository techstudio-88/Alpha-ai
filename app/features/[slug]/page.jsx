const pages={clipping:["AI Video Clipping","Find strong moments in long videos and turn them into short-form candidates.","/ai-video-clipping"],captions:["AI Captions","Generate and render readable captions around the moments that matter.","/ai-caption-generator"],editor:["AI Video Editor","Edit, reframe, caption and render short-form clips in one workspace.","/ai-video-editor"],repurposing:["Video Repurposing","Turn one long-form source into multiple content-ready outputs.","/ai-repurposing-tool"]};export default async function Page({params}){const p=await params;const x=pages[p.slug]||pages.clipping;return <main style={{maxWidth:900,margin:"0 auto",padding:"72px 24px",fontFamily:"Arial,sans-serif"}}><a href="/">Alpha.ai</a><h1>{x[0]}</h1><p style={{fontSize:22}}>{x[1]}</p><a href="/auth">Start with Alpha.ai →</a></main>}export function generateStaticParams(){return Object.keys(pages).map(slug=>({slug}))}