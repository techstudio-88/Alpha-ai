"use client";import{useState}from"react";import{createClient}from"@supabase/supabase-js";export default function Page(){const[c,setC]=useState("");const[w,setW]=useState("");const[o,setO]=useState(null);async function create(){const s=await createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY||process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY).auth.getSession();const r=await fetch("/api/referrals",{method:"POST",headers:{Authorization:"Bearer "+s.data.session?.access_token,"content-type":"application/json"},body:JSON.stringify({workspace_id:w,code:c})});setO(await r.json())}return <main className="seo-page"><h1>Referral Program</h1><p>Create a shareable referral code for your workspace.</p><input value={w} onChange={e=>setW(e.target.value)} placeholder="Workspace ID"/><input value={c} onChange={e=>setC(e.target.value)} placeholder="Optional referral code"/><button onClick={create}>Create referral</button>{o&&<pre>{JSON.stringify(o,null,2)}</pre>}</main>}