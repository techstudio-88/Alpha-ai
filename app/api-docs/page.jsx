export default function Page(){return <main className="seo-page"><h1>Alpha.ai API</h1><p>Use an Alpha API key to read clips from its authorized workspace.</p><h2>Authentication</h2><pre>X-API-Key: alpha_...</pre><h2>List clips</h2><pre>GET /api/v1/projects/:projectId/clips</pre><h2>Create a key</h2><pre>{`POST /api/v1/keys\\nAuthorization: Bearer &lt;Supabase session token&gt;\\n{ "workspace_id": "..." }`}</pre><p>Keys are hashed server-side. Never expose a service-role key in browser code.</p></main>}