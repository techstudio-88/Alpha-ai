import{createClient}from"@supabase/supabase-js";const a=()=>createClient(process.env.SUPABASE_URL||process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SECRET_KEY,{auth:{persistSession:false}});export async function GET(){const {data,error}=await a().from("creator_case_studies").select("slug,title,creator_name,channel_name,niche,summary,before_metrics,after_metrics,content").eq("published",true).order("created_at",{ascending:false});if(error)return Response.json({error:error.message},{status:500});return Response.json(data||[])}