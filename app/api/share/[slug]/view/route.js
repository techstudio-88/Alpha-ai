import { createClient } from "@supabase/supabase-js";export async function POST(req,{params}){try{const {slug}=await params;const c=createClient(process.env.SUPABASE_URL||process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SECRET_KEY,{auth:{persistSession:false}});const {data}=await c.from("public_shares").select("id,view_count,enabled").eq("slug",slug).eq("enabled",true).maybeSingle();if(!data)return Response.json({error:"Not found"},{status:404});await c.from("public_shares").update({view_count:(data.view_count||0)+1}).eq("id",data.id);return Response.json({ok:true,view_count:(data.view_count||0)+1})}catch(e){return Response.json({error:e.message},{status:500})}}