import crypto from"node:crypto";import{createClient}from"@supabase/supabase-js";const a=()=>createClient(process.env.SUPABASE_URL||process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SECRET_KEY,{auth:{persistSession:false}});export async function POST(req){try{const auth=req.headers.get("authorization")||"";if(!auth.startsWith("Bearer "))return Response.json({error:"Unauthorized"},{status:401});const pub=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY||process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;const u=await createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,pub,{auth:{persistSession:false}}).auth.getUser(auth.slice(7));if(!u.data.user)return Response.json({error:"Unauthorized"},{status:401});const b=await req.json();if(!b.workspace_id)return Response.json({error:"workspace_id required"},{status:400});const svc=a();const {data:member}=await svc.from("workspace_members").select("role").eq("workspace_id",b.workspace_id).eq("user_id",u.data.user.id).maybeSingle();if(!member)return Response.json({error:"Workspace access denied"},{status:403});const raw="alpha_"+crypto.randomBytes(24).toString("hex");const hash=crypto.createHash("sha256").update(raw).digest("hex");const {data,error}=await a().from("api_keys").insert({workspace_id:b.workspace_id,user_id:u.data.user.id,name:String(b.name||"API key").slice(0,100),key_prefix:raw.slice(0,16),key_hash:hash}).select("id,name,key_prefix,created_at").single();if(error)throw error;return Response.json({...data,key:raw})}catch(e){return Response.json({error:e.message},{status:500})}}