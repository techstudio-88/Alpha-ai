import{createClient}from"@supabase/supabase-js";import crypto from"node:crypto";const db=()=>createClient(process.env.SUPABASE_URL||process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SECRET_KEY,{auth:{persistSession:false}});function sign(v){return crypto.createHmac("sha256",process.env.OAUTH_STATE_SECRET||process.env.CRON_SECRET||"alpha-oauth-state").update(v).digest("hex")}export async function GET(req){const u=new URL(req.url),platform=u.searchParams.get("platform"),workspace=u.searchParams.get("workspace_id");if(platform!=="youtube")return Response.json({error:"Provider connection currently supports YouTube; Instagram/TikTok adapters require their provider review/credentials."},{status:400});if(!workspace)return Response.json({error:"workspace_id required"},{status:400});const client=process.env.YOUTUBE_CLIENT_ID;if(!client)return Response.json({error:"YOUTUBE_CLIENT_ID is not configured."},{status:503});const state=Buffer.from(JSON.stringify({workspace_id:workspace,nonce:crypto.randomBytes(12).toString("hex")})).toString("base64url"),signed=state+"."+sign(state),redirect=process.env.YOUTUBE_REDIRECT_URI||new URL("/api/publish/callback",u.origin).toString();const params=new URLSearchParams({client_id:client,redirect_uri:redirect,response_type:"code",scope:"https://www.googleapis.com/auth/youtube.upload https://www.googleapis.com/auth/youtube.readonly",access_type:"offline",prompt:"consent",state:signed});return Response.redirect("https://accounts.google.com/o/oauth2/v2/auth?"+params.toString())}