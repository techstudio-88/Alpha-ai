export async function POST(req){try{const b=await req.json();const tool=String(b.tool||"");const input=String(b.input||"").slice(0,6000);if(!input)return Response.json({error:"Input required"},{status:400});const prompts={hook:"Generate 10 short-form hooks for this content. Return a numbered list.",title:"Generate 10 short-form video titles. Return a numbered list.",caption:"Write 5 concise social captions for this content.",ideas:"Give 8 distinct short-form clip ideas from this content, each with a hook angle.",podcast:"Create a podcast-to-shorts plan with 8 clip angles and suggested durations."};const prompt=prompts[tool]||prompts.ideas;const r=await fetch("https://generativelanguage.googleapis.com/v1beta/models/"+encodeURIComponent(process.env.GEMINI_MODEL||"gemini-3.8-flash")+":generateContent?key="+encodeURIComponent(process.env.GEMINI_API_KEY||""),{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({contents:[{parts:[{text:prompt+"\nContent:\n"+input}]}]})});const j=await r.json();if(!r.ok)throw new Error(j?.error?.message||"AI provider error");return Response.json({ok:true,text:j?.candidates?.[0]?.content?.parts?.map(x=>x.text||"").join("")||""})}catch(e){return Response.json({error:e.message||"Tool failed"},{status:500})}}